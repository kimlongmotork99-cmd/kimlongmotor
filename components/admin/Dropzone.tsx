'use client'
import {useRef,useState} from 'react'
import {supabaseBrowser} from '@/lib/supabase'
export default function Dropzone({onUploaded,accept='image/*',label='Kéo & thả tệp vào đây'}:{onUploaded:(url:string,name:string)=>void;accept?:string;label?:string}){const input=useRef<HTMLInputElement>(null);const [busy,setBusy]=useState(false);const [drag,setDrag]=useState(false);const upload=async(file:File)=>{setBusy(true);const supabase=supabaseBrowser();const safe=file.name.toLowerCase().replace(/[^a-z0-9._-]/g,'-');const path=`${Date.now()}-${safe}`;const {error}=await supabase.storage.from('media').upload(path,file,{upsert:false,contentType:file.type});if(error){alert(error.message);setBusy(false);return}const {data}=supabase.storage.from('media').getPublicUrl(path);await supabase.from('media').insert({name:file.name,url:data.publicUrl,type:file.type.includes('pdf')?'pdf':'image',size_bytes:file.size});onUploaded(data.publicUrl,file.name);setBusy(false)};return <div className={'dropzone '+(drag?'drag':'')} onDragOver={e=>{e.preventDefault();setDrag(true)}} onDragLeave={()=>setDrag(false)} onDrop={e=>{e.preventDefault();setDrag(false);const f=e.dataTransfer.files[0];if(f)upload(f)}} onClick={()=>input.current?.click()}><input ref={input} type="file" hidden accept={accept} onChange={e=>{const f=e.target.files?.[0];if(f)upload(f)}}/><div className="drop-icon">↥</div><b>{busy?'Đang tải lên…':label}</b><span>hoặc bấm để chọn tệp</span></div>}
