'use client'
export type Spec={label:string;value:string}
export default function SpecTable({value,onChange}:{value:Spec[];onChange:(v:Spec[])=>void}){const rows=value.length?value:[{label:'',value:''}];const set=(i:number,k:keyof Spec,v:string)=>onChange(rows.map((r,n)=>n===i?{...r,[k]:v}:r));return <div className="spec-table"><div className="spec-head"><span>THÔNG SỐ</span><span>GIÁ TRỊ</span><span></span></div>{rows.map((r,i)=><div className="spec-row" key={i}><input value={r.label} onChange={e=>set(i,'label',e.target.value)} placeholder="Ví dụ: Động cơ"/><input value={r.value} onChange={e=>set(i,'value',e.target.value)} placeholder="Ví dụ: YUCHAI"/><button type="button" onClick={()=>onChange(rows.filter((_,n)=>n!==i))}>×</button></div>)}<button className="btn secondary" type="button" onClick={()=>onChange([...rows,{label:'',value:''}])}>+ Thêm dòng</button></div>}
