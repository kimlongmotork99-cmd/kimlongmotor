import {supabaseServer} from '@/lib/supabase-server'; import ProductEditor from '@/components/ProductEditor'
export default async function EditProduct({params}:{params:Promise<{id:string}>}){const {id}=await params;const sb=await supabaseServer();const {data,error}=await sb.from('products').select('*').eq('id',id).single();if(error||!data)return <div className="admin-empty">Không tìm thấy sản phẩm.</div>;return <div className="admin-page"><div className="page-head"><div><span className="eyebrow">SẢN PHẨM</span><h1>Chỉnh sửa {data.name}</h1><p>Cập nhật nội dung, hình ảnh, thông số và catalogue.</p></div></div><ProductEditor initial={data}/></div>}
