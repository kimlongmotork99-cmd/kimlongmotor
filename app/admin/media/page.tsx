'use client'; import {useEffect,useState} from 'react'; import {supabaseBrowser} from '@/lib/supabase'; import Dropzone from '@/components/admin/Dropzone'; import RoleGate from '@/components/admin/RoleGate'
export default function Media(){const [items,setItems]=useState<any[]>([]);const load=()=>supabaseBrowser().from('media').select('*').order('created_at',{ascending:false}).then(({data})=>setItems(data||[]));useEffect(()=>{load()},[]);const remove=async(id:string)=>{if(!confirm('Xóa media này?'))return;await supabaseBrowser().from('media').delete().eq('id',id);load()};return <RoleGate allow={['admin','editor']}><div className="admin-page"><div className="page-head"><div><span className="eyebrow">MEDIA LIBRARY</span><h1>Thư viện nội dung</h1><p>Kéo-thả ảnh hoặc catalogue PDF. URL được lưu vào Supabase Storage.</p></div></div><div className="panel"><Dropzone onUploaded={()=>load()} label="Kéo & thả ảnh / PDF vào đây" accept="image/*,.pdf"/></div><div className="media-grid">{items.map(x=><div className="media-card" key={x.id}>{x.type==='pdf'?<div className="pdf-box">PDF</div>:<img src={x.url} alt={x.alt||x.name}/>}<div className="media-info"><b>{x.name}</b><small>{x.type}</small><button className="icon-btn" onClick={()=>remove(x.id)}>×</button></div></div>)}</div></div></RoleGate>}
