import {supabaseServer} from '@/lib/supabase-server'; import RoleGate from '@/components/admin/RoleGate'; import Link from 'next/link'; import DeleteButton from '@/components/DeleteButton'
export default async function AdminNews(){const sb=await supabaseServer();const {data}=await sb.from('news').select('*').order('created_at',{ascending:false});return <RoleGate allow={['admin','editor']}><div className="admin-page"><div className="page-head"><div><span className="eyebrow">CONTENT</span><h1>Tin tức</h1><p>Quản lý bài viết, sự kiện và hình ảnh.</p></div><Link className="btn primary" href="/admin/news/new">+ Thêm bài</Link></div><div className="panel"><div className="table-wrap"><table className="data-table"><thead><tr><th>Tiêu đề</th><th>Chuyên mục</th><th>Trạng thái</th><th></th></tr></thead><tbody>{data?.map(n=><tr key={n.id}><td><strong>{n.title}</strong><small>/{n.slug}</small></td><td>{n.category}</td><td><span className={'status '+n.status}>{n.status==='published'?'Đã xuất bản':'Bản nháp'}</span></td><td><Link href={`/admin/news/${n.id}`}>Sửa →</Link> <DeleteButton table="news" id={n.id}/></td></tr>)}</tbody></table></div></div></div></RoleGate>}
