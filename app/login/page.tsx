'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {supabaseBrowser} from '@/lib/supabase'
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const router=useRouter(); const go=async(e:any)=>{e.preventDefault();setError('');const {error}=await supabaseBrowser().auth.signInWithPassword({email,password});if(error)setError(error.message);else router.push('/admin')};return <main className="login"><div className="panel"><div className="logo">KIM LONG<small>MOTOR</small></div><h2>CMS quản trị</h2><p className="muted">Đăng nhập để quản lý sản phẩm, tin tức, hình ảnh và khách hàng.</p><form className="form" onSubmit={go}><input className="field" type="email" placeholder="Email quản trị" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="field" type="password" placeholder="Mật khẩu" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="btn primary">ĐĂNG NHẬP →</button>{error&&<p style={{color:'crimson'}}>{error}</p>}</form></div></main>}
